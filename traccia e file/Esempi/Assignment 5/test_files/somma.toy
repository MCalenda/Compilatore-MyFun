proc somma(int x,y) int:
    int z := x;
    int i := 1;

    while i <= y do
        z := z + 1;
        i := i + 1;
    od;

    -> z
corp;

proc main(int number1, number2) int:
    int x;
    int y;
    int z;

    x := number1;
    y := number2;

    z := somma(x, y);

    write(x);
    write(" + ");
    write(y);
    write(" = ");
    write(z);

    -> 0
corp;
